tasks:

  runMlopsScript:
    private: true
    extend: core.runShellScript
    location: ../bash

  addData1:
    extend: runMlopsScript
    config:
      start: ./addData1.sh

  addData2:
    extend: runMlopsScript
    config:
      start: ./addData2.sh

  addData3:
    extend: runMlopsScript
    config:
      start: ./addData3.sh

  predict0:
    extend: runMlopsScript
    config:
      start: ./predict0.sh

  predict1:
    extend: runMlopsScript
    config:
      start: ./predict1.sh

  predict2:
    extend: runMlopsScript
    config:
      start: ./predict2.sh
